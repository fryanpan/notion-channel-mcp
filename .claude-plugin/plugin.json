{
  "name": "notion-channel-mcp",
  "version": "0.2.0",
  "description": "Notion comment webhooks as Claude Code channel notifications",
  "channels": [
    {
      "server": "notion-channel"
    }
  ],
  "mcpServers": {
    "notion-channel": {
      "command": "/bin/sh",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/bin/notion-channel-mcp.sh",
        "${CLAUDE_PLUGIN_ROOT}/server.ts"
      ]
    }
  }
}
